#! load-cf-deployment.yml
#! Load the base cf-deployment.yml from vendored directory
#!
#! This file is a simple passthrough that makes ytt process the cf-deployment.yml
#! manifest so that other overlay files can modify it.
#!
#! Usage: Include this file FIRST when running ytt:
#!   ytt -f manifests/cf-deployment/cf-deployment.yml \
#!       -f manifests/templates/base/apply-*.yml \
#!       -f manifests/templates/values.yml
#!
#! Note: This file serves as a placeholder/documentation.
#! In practice, we directly include cf-deployment.yml in the ytt command.
